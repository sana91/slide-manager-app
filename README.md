# Slide Manager App

HTMLスライドを管理・登録・閲覧できるアプリケーション

## 🚀 技術スタック

- **Nuxt 3** - Vue.jsフレームワーク（SPAモード）
- **Vue 3** - Composition API
- **TypeScript** - 型安全性
- **Tailwind CSS** - ユーティリティファーストCSS（Arbitrary Values対応）
- **Composables + Provide/Inject** - 状態管理

## 📐 アーキテクチャ

### 設計原則

- **関心の分離**: ビジネスロジックとUIコンポーネントの完全な分離
- **状態管理**: Composables + Provide/Injectパターン
- **型安全性**: TypeScriptによる厳格な型チェック
- **SPAモード**: クライアントサイドのみで動作

### ディレクトリ構造

```
slide-manager-app/
├── assets/              # CSS、画像などの静的アセット
│   └── css/
│       └── tailwind.css # Tailwind CSSのエントリーポイント
├── components/          # 再利用可能なVueコンポーネント
├── composables/         # ビジネスロジックとステート管理
│   ├── useSlides.ts           # スライド管理ロジック
│   └── useSlideProvider.ts    # Provide/Inject設定
├── layouts/             # レイアウトコンポーネント
│   └── default.vue      # デフォルトレイアウト
├── pages/               # ルーティングページ
│   └── index.vue        # トップページ
├── plugins/             # Nuxtプラグイン
├── public/              # 公開静的ファイル
├── types/               # TypeScript型定義
│   └── index.ts         # 共通型定義
├── stores/              # 追加の状態管理（必要に応じて）
├── app.vue              # ルートコンポーネント
├── nuxt.config.ts       # Nuxt設定
├── tailwind.config.ts   # Tailwind設定
└── tsconfig.json        # TypeScript設定
```

## 🔧 セットアップ

### 1. 依存関係のインストール

```bash
npm install
```

### 2. 開発サーバーの起動

```bash
npm run dev
```

開発サーバーは `http://localhost:3000` で起動します。

### 3. ビルド

```bash
npm run build
```

### 4. プレビュー

```bash
npm run preview
```

## 💡 機能

- ✅ スライドの登録・管理
- ✅ スライド一覧表示
- ✅ スライド閲覧機能
- ✅ フィルタリング・検索
- ✅ タグ管理
- ✅ ローカルストレージによるデータ永続化

## 📝 開発ガイドライン

### Composablesの使用方法

```typescript
// コンポーネント内でスライド管理機能を使用
const slideContext = injectSlideContext()

// スライド一覧を取得
const slides = slideContext.filteredSlides

// 新規スライド作成
await slideContext.createSlide({
  title: 'My Slide',
  htmlContent: '<div>Content</div>'
})

// スライド更新
await slideContext.updateSlide(slideId, {
  title: 'Updated Title'
})

// スライド削除
await slideContext.deleteSlide(slideId)
```

### Tailwind Arbitrary Valuesの活用

```vue
<template>
  <div 
    class="w-[calc(100%-2rem)] h-[50vh] bg-[#1a1a1a]"
    :class="[`p-[${spacing}px]`]"
  >
    <!-- 柔軟なスタイリング -->
  </div>
</template>
```

### 型定義の使用

```typescript
import type { Slide, CreateSlideInput, UpdateSlideInput } from '~/types'

const newSlide: CreateSlideInput = {
  title: 'タイトル',
  description: '説明',
  htmlContent: '<div>コンテンツ</div>',
  tags: ['tag1', 'tag2']
}
```

## 🎯 状態管理の仕組み

### Provide/Injectパターン

```vue
<!-- layouts/default.vue -->
<script setup>
// レイアウトレベルでコンテキストを提供
const slideContext = provideSlideContext()
</script>

<!-- pages/some-page.vue -->
<script setup>
// 任意のコンポーネントでコンテキストを注入
const slideContext = injectSlideContext()
</script>
```

### useState による状態管理

- グローバルな状態は `useState` で管理
- SSRフレンドリー（SPAモードでも動作）
- リアクティブな状態更新

## 🔐 型安全性

- TypeScriptの厳格モードを有効化
- 全ての状態とメソッドに型定義
- コンポーネント間のデータフローを型で保証

## 📦 今後の拡張

- [ ] バックエンドAPI連携
- [ ] 認証・認可機能
- [ ] スライドのプレゼンテーションモード
- [ ] スライドのエクスポート機能
- [ ] マルチメディア対応（画像、動画の埋め込み）
- [ ] コラボレーション機能

## 📄 ライセンス

MIT
