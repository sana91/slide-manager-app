{
  "name": "slide-manager-app",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "npm run generate:data && nuxt dev",
    "generate": "nuxt generate",
    "generate:slides": "node scripts/generate-slides.cjs",
    "generate:missing": "node scripts/generate-missing-slides.cjs",
    "generate:data": "node scripts/generate-slide-data.cjs",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.15.0",
    "vue": "latest",
    "vue-router": "latest",
    "isomorphic-dompurify": "^2.16.0",
    "chart.js": "^4.4.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.2",
    "typescript": "^5.7.2"
  }
}
